const config = {
    user: "root",
    password: "root", // leave this blank for windows
    port: 8889, // for window 3306
    host: "localhost",
    database: "marvelcharacters"
}

module.exports = config;